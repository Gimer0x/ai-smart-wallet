# Backend environment (copy to .env and fill in)
# Required for user auth and Circle
CIRCLE_API_KEY=your_circle_api_key
GOOGLE_CLIENT_ID=your_google_web_client_id
GROQ_API_KEY=your_groq_api_key
SESSION_SECRET=your_session_secret

# Same Google OAuth client ID as frontend VITE_GOOGLE_CLIENT_ID (for verifying ID tokens)
# GOOGLE_WEB_CLIENT_ID=...  # alternative env name

